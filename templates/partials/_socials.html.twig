<div class="social">
    {% if socials is defined and socials|length > 0 %}
        {% for social in socials %}
            <a class="social__link" href="{{ social.url }}"><i class="{{ social.iconClass }}"></i></a>
        {% endfor %}
    {% else %}
        <a class="social__link" href="#"><i class="feathericon-link"></i></a>
    {% endif %}
</div>
