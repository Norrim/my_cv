# Contribution Guidelines

## Branches
- `main` : production (déployée automatiquement)
- `feature/*` : nouvelles features
- `fix/*` : corrections
- `hotfix/*` : correctifs urgents

Toute modification doit passer par une Pull Request vers `main`.

---

## Qualité requise
Avant d’ouvrir une PR :

```bash
make qa
